spring:
  application:
    name: discodeit
  boot:
    admin:
      client:
        url: ${SPRING_BOOT_ADMIN_CLIENT_URL}
  datasource:
    url: jdbc:postgresql://localhost:5432/discodeit
    username: discodeit_user
    password: discode1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: false
    open-in-view: false

logging:
  level:
    com:
      sprint:
        mission:
          discodeit: INFO
    org:
      hibernate:
        SQL: INFO

discodeit:
  storage:
    type: local
    local:
      root-path: ./discodeit/local/storage/prod
